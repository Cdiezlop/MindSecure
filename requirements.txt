# Requerimientos para el backend Django y la base de datos (SQLite)

# Framework principal
Django>=4.2

# Django REST Framework para crear APIs
djangorestframework>=3.14

# Seguridad y autenticación
python-decouple>=3.8
django-guardian>=2.4.0

# Autenticación opcional (si se habilita allauth)
django-allauth>=0.54

# Almacenamiento futuro (por ejemplo, PDF)
django-storages>=1.14

# PostgreSQL (solo si se llega a usar)
# psycopg2-binary>=2.9.6

# Permitir peticiones desde el frontend (Vue)
django-cors-headers>=4.3.0

# NOTA: Instalar con:
# pip install -r requirements.txt
